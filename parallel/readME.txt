
0. type "man module" to see the list of options.
   can use "module reset" to restore default modules

1. Need to load mkl and openmpi modules before compiling
   module --force purge (this will remove all default modules)
   # module load openmpi (NOT needed anymore)
   module load intel
   module load mkl
